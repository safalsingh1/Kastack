# ConvoRAG – Conversation Intelligence System

A fully local RAG (Retrieval-Augmented Generation) system that analyses a large conversation dataset, detects topic changes chronologically, extracts a user persona, and powers an interactive chatbot backed by **Groq LLM** (llama-3.3-70b-versatile) + **FAISS** semantic search.

---

## 📁 Project Structure

```
kastack/
├── conversations.csv       # Input dataset (one conversation per row)
├── requirements.txt
├── .env                    # Put your GROQ_API_KEY here
│
├── data_processor.py       # Parse CSV → flat message stream
├── topic_detector.py       # Detect topic boundaries via sentence embeddings
├── checkpoint_manager.py   # Build topic & 100-msg checkpoints with summaries
├── persona_extractor.py    # Extract habits, traits, facts from conversations
├── rag_engine.py           # FAISS retrieval + Groq LLM answer generation
├── build_index.py          # One-time pipeline: run this first
├── app.py                  # FastAPI API + serve UI
│
├── data/                   # Auto-generated by build_index.py
│   ├── messages.json
│   ├── checkpoints.json
│   ├── persona.json
│   ├── embeddings.npy
│   ├── topic_index.faiss
│   └── msg_index.faiss
│
└── static/
    ├── index.html          # Chatbot UI
    ├── style.css
    └── app.js
```

---

## 🚀 Quick Start

### 1. Install dependencies
```bash
pip install -r requirements.txt
python -m spacy download en_core_web_sm
```

### 2. Set your Groq API key
Edit `.env`:
```
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxx
```
Get a free key at https://console.groq.com

### 3. Build the index (run once, ~3–8 min on first run)
```bash
python build_index.py
```
This will:
- Parse all conversations into a flat message stream
- Detect topic boundaries
- Generate topic + 100-msg checkpoint summaries
- Build two FAISS vector indexes
- Extract the user persona

### 4. Start the chatbot
```bash
python app.py
```
Open http://localhost:5000
API interactive docs are available at http://localhost:5000/docs

---

## 🧠 How It Works

### Part 1 — RAG System with Checkpoints

#### Topic Detection
Messages are processed **chronologically** in their original CSV order.

Each message is embedded using `sentence-transformers` (`all-MiniLM-L6-v2`).  
A **sliding window of 5 messages** is averaged into a single window vector.  
We compute the **cosine similarity** between consecutive windows.  
When similarity drops below **threshold 0.40**, we mark a **topic boundary**.

```
Window 1 (msgs 0–4)  ←→  Window 2 (msgs 5–9)   → similarity = 0.72  (same topic)
Window 2 (msgs 5–9)  ←→  Window 3 (msgs 10–14) → similarity = 0.28  (NEW TOPIC ✓)
```

Each topic segment gets:
- A **summary** (extractive TF-IDF sentence ranking, no API)
- A **keyword list** (top-N non-stopword tokens)
- A **FAISS vector** for semantic retrieval

#### 100-Message Checkpoints
Every 100 global messages, a checkpoint is created **independently** of topic boundaries. Same extractive summary + FAISS index.

#### Retrieval
At query time:
1. Query is embedded with the same model
2. **Top-5 checkpoint summaries** retrieved (cosine similarity on topic+100-msg index)
3. **Top-8 message snippets** retrieved (sampled message index)
4. Context fed to **Groq llama-3.3-70b-versatile** for answer generation

---

### Part 2 — Persona Extraction

All signals extracted locally via **regex + keyword matching**:

| Category | Method |
|---|---|
| **Habits** | Pattern matching: "wake up", "work out", "read", "cook", etc. |
| **Personal facts** | Regex for jobs, family signals, locations, pets, hobbies |
| **Personality traits** | Keyword frequency scoring: positive, empathetic, curious, etc. |
| **Communication style** | Avg message length, emoji ratio, question ratio, tone analysis |

Stored in `data/persona.json`.

---

### Part 3 — Chatbot

The FastAPI backend routes each query:

```
Persona question? → Direct answer from persona.json (instant, no LLM)
               ↓
         RAG retrieval → FAISS checkpoint search + message search
               ↓
         Groq llama-3.3-70b generates grounded answer from context
```

Persona shortcut examples:
- *"What are their habits?"* → reads `persona.habits` directly
- *"How do they communicate?"* → reads `persona.communication_style`
- *"What kind of person is this user?"* → reads `persona.personality_traits`

For open-ended questions, full RAG+Groq is used.

---

## 🖥️ UI Features

- **Dark glassmorphism** design
- **Topic timeline** sidebar — click any topic to ask about it
- **Persona panel** — full structured view of extracted persona
- **Source badges** on every bot answer
- Supports markdown-style **bold** and bullet lists in answers

---

## 📦 Key Dependencies

| Library | Purpose |
|---|---|
| `sentence-transformers` | Local message/query embeddings |
| `faiss-cpu` | Fast vector similarity search |
| `groq` | LLM answer generation (llama-3.3-70b-versatile) |
| `fastapi` | Web API + UI server |
| `uvicorn` | ASGI server |
| `spacy` | NLP for persona extraction |
| `python-dotenv` | Env variable management |

---

## ⚡ Performance Notes

- Embeddings are **cached** in `data/embeddings.npy` — only computed once
- Message index samples **every 5th message** for speed (still covers full dataset)
- Groq inference is extremely fast (~0.5–2s response time)
- Persona questions bypass LLM entirely for **instant** responses

---

## 📝 Example Queries

```
"What kind of person is this user?"
"What are their habits?"
"How do they communicate?"
"What hobbies come up most often?"
"Do they talk about music?"
"Tell me about their family"
"What jobs are mentioned in the conversations?"
```
